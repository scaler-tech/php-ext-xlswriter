# Exceptions

xlswriter throws `Vtiful\Exception\*` exceptions when an operation fails. The exception code can be used by application code to handle specific failure modes.

* [Exception codes](code.md)
