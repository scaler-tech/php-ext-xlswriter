{
  "private": true,
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "docs:build": "honkit build docs _site && node -e \"const fs = require('fs'); fs.closeSync(fs.openSync('docs/_site/.nojekyll', 'w'));\"",
    "docs:serve": "honkit serve docs _site"
  },
  "devDependencies": {
    "honkit": "^6.1.7"
  }
}
